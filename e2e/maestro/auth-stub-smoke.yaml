# Aoi Maestro E2E (stub mode only)
#
# Run against a stub-mode dev build (EXPO_PUBLIC_AUTH_STUB_MODE=true) on an
# emulator: `maestro test e2e/maestro/<flow>.yaml`
#
# Deliberately NOT covered here (no reliable automation — see MANUAL.md):
# photo/voice capture (native pickers), real purchases, cross-device partner
# join, push delivery, PDF share sheets.

appId: com.ekasc.aoi
---
- launchApp:
    clearState: true
- assertVisible: "Continue to your private space"
- tapOn: "Continue with Google"
- assertVisible: "Save the little things"
